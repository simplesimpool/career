import { RealtimeSession } from './realtime/client';
import { ConnectionConfig } from './config/connection';
import { BatchTranscription } from './batch';
export declare class Speechmatics {
    private _connectionConfig;
    private _batch;
    constructor(config: string | ConnectionConfig);
    get batch(): BatchTranscription;
    realtime(config?: string | ConnectionConfig): RealtimeSession;
}
