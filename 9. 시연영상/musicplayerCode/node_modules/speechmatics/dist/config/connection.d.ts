export declare class ConnectionConfigFull implements ConnectionConfig {
  apiKey: string | (() => Promise<string>);
  batchUrl: string;
  realtimeUrl: string;
  managementPlatformUrl: string;
  appId?: string;
  constructor(config: string | ConnectionConfig);
}
export interface ConnectionConfig {
  apiKey: string | (() => Promise<string>);
  batchUrl?: string;
  realtimeUrl?: string;
  managementPlatformUrl?: string;
  appId?: string;
}
