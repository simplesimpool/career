<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="doctorlicensens">
	<select id="select" parameterType="string" resultType="doctorlicense">
		select * from doctorLicense where dr_lic_code=#{code} 
	</select>
</mapper>